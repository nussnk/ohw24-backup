- hosts: client
  user: ansible
  become: true
  tasks:
    - name: Enable EPEL Repository
      yum:
        name: epel-release
        state: latest
    - name: Install borgbackup
      yum:
        name: borgbackup
        state: latest

